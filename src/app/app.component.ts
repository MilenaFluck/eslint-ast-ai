import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  standalone: true,
  styleUrl: './app.component.css'
})
export class AppComponent {
  /*** Input-Felder
   - Framework (default: none): Angular, React, none
   - Description (required): Textarea
   - Category (optional): Angular, Best Practices, Possible Errors, Security, Performance, Deprecated/Legacy, Typescript, Test-related, Code Quality, Stylistic Issues, React
   - Type (default: problem): problem, suggestion, layout
   - Failure example (optional): Code-Editor (provided example where rule should throw error)
   - Add fixable (default: false): Checkbox (true/false)
   ***/
}

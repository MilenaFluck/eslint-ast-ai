# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build rule-creator-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t rule-creator-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system rule-creator-api && \
          adduser --system -G rule-creator-api rule-creator-api

COPY dist/apps/rule-creator-api rule-creator-api/
RUN chown -R rule-creator-api:rule-creator-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix rule-creator-api --omit=dev -f install

CMD [ "node", "rule-creator-api" ]

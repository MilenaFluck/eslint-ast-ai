# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build rule-creator-web`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t rule-creator-web`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system rule-creator-web && \
          adduser --system -G rule-creator-web rule-creator-web

COPY dist/apps/rule-creator-web rule-creator-web/
RUN chown -R rule-creator-web:rule-creator-web .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix rule-creator-web --omit=dev -f install

CMD [ "node", "rule-creator-web" ]
